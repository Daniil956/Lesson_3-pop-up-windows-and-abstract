n = “hello” - что бы писать строки

n = 123; - что бы записать число 

Infinity представляет собой математическую бесконечность - alert(5/0);//Infinity

NaN (Not a Number) означает вычислительную ошибку -  alert (”не число”/4);//NaN

Булевский тип

lrt age = null; - Значение пустое 

Let age; - undefined - показывает что значение не было задано.
alert(age);// выдает “undefined”

typeoff - позваляет узнать какая у нас элемент. 

let text = prompt(”Test”) - вылазиет окошко как мастер пороль. Значение которое принято будет записано в Test.

let right = confirm(”ты всегда прав?”) - вылезает окошко которое спрашивает тебе и есть ответ “да” или “нет”

let number = parseInt(prompt(”Enter number”)); alert(number+1) - Феолетовое - переводит тип в число. Оранжевое - прибавляет 1.